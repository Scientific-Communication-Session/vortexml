flask>=3.0
flask-socketio>=5.3
eventlet>=0.35
torch>=2.0
pandas>=2.0
openpyxl>=3.1
scikit-learn>=1.3
numpy>=1.24
flask-cors>=4.0
flask-sqlalchemy>=3.1
# Rate limiting for auth + paid-LLM endpoints (brute-force / cost-abuse guard).
flask-limiter>=3.5
psycopg2-binary>=2.9
# Floor pinned to a release that supports output_config.effort, adaptive
# thinking, and the claude-sonnet-4-6 / claude-opus-4-7 model IDs used by the
# chatbot + auto-configure endpoints. Older SDKs raise TypeError on these.
anthropic>=0.102
psutil>=5.9
# ONNX export of trained models (POST .../export?format=onnx). TorchScript
# export needs nothing beyond torch.
onnx>=1.16
# RAG document ingestion (PDF). txt/md/csv need nothing extra.
pypdf>=4.0
# Broad file-format ingestion for both the dataset upload (table extraction) and
# RAG document upload (text extraction). Each is imported lazily inside its
# conversion branch and guarded with a friendly error if missing.
python-docx>=1.1      # Word .docx → tables (datasets) / text (RAG)
python-pptx>=1.0      # PowerPoint .pptx → slide text (RAG)
pyarrow>=15.0         # Parquet .parquet → DataFrame (datasets)
beautifulsoup4>=4.12  # HTML tag-stripping (RAG)
lxml>=5.0             # HTML table parsing via pandas.read_html (datasets)
striprtf>=0.0.26      # RTF .rtf → text (RAG)
# RAG retrieval defaults to TF-IDF (scikit-learn, already above) and generation
# can use the cloud (anthropic) key. The local generation backends are OPTIONAL
# and intentionally NOT hard requirements (heavy / platform-specific):
#   mlx-lm                 # Apple Silicon — recommended local backend
#   llama-cpp-python       # GGUF, cross-platform
#   transformers accelerate# any HF model via PyTorch
#   sentence-transformers  # MiniLM semantic embeddings (better retrieval)
# Install whichever you want; the Assistant detects what's available.
